---
title: 裝新系統 個人設定
tags: []
slug: system-settings
date: 1970-01-01
lastmod: 2026-08-29
---
### Vim

> \~/.vimrc

1. 安裝vim-gtk3
2. set .vimrc

```bash
set tabstop=4
set nu
set shiftwidth=4
set expandtab
set clipboard=unnamedplus
set foldmethod=indent
set laststatus=2
set statusline=%f
```

### Keyboard

[keyd](/posts/cs/linux/keyd)

```
git clone https://github.com/rvaiya/keyd
cd keyd
make && sudo make install
sudo systemctl enable --now keyd
```

`sudo vim /etc/keyd/default.conf`

```
[ids]
*

[main]
# 將 Caps Lock 映射為 Escape
capslock = esc

# 將原本的 Escape 映射為 Grave (即 ` 鍵)
esc = `  
# 強制定義 Right Shift，防止被誤認為 Left Shift
rightshift = rightshift
```

```
sudo keyd reload
```

### Tmux

> .tmux.conf

1. C-b to C-a
2. C-hjkl to move

```bash
set -g prefix C-a
set -g default-terminal "xterm-256color"

bind -n C-h select-pane -L
bind -n C-j select-pane -D
bind -n C-k select-pane -U
bind -n C-l select-pane -R
bind r source-file ~/.tmux.conf

set -sg escape-time 0
set -g mouse on
# statusbar
set -g status-position bottom
set -g status-justify left
set -g status-style 'fg=red'

set -g status-left ''
set -g status-left-length 10

set -g status-right-style 'fg=black bg=yellow'
set -g status-right '%Y-%m-%d %H:%M '
set -g status-right-length 50

setw -g window-status-current-style 'fg=black bg=red'
setw -g window-status-current-format ' #I #W #F '

setw -g window-status-style 'fg=red bg=black'
setw -g window-status-format ' #I #[fg=white]#W #[fg=yellow]#F '

setw -g window-status-bell-style 'fg=yellow bg=red bold'
setw -g mode-keys vi

bind -T copy-mode-vi c send -X copy-pipe-and-cancel "xclip -selection clipboard"
bind-key -T copy-mode-vi Y send -X copy-line
bind r source-file ~/.tmux.conf \; display "Reloaded!"
```

### Ranger

> .config/ranger/rc.conf

1. yp settings (xclip needed)

```bash
map yp shell printf %d/%f | xclip -selection clipboard
map yn shell printf %f | xclip -selection clipboard 
```

### bashrc

> \~/.bashrc\
> 自由調整
